Meta (Instagram / Facebook) music library. Empty on purpose until tracks cleared for
Meta are dropped in: with Publish:Music:Required=true (the default) a Meta target
is refused rather than published silent. YouTube tracks live in ../youtube/ and
are NOT interchangeable — different licence terms per platform.

# LifeOverYears — working notes for Claude

One photo of an ordinary American commercial place → six photorealistic frames
(1975–2025) → a vertical video with a caption. The whole product is *prompt
text*: almost every behaviour question ("why is there X in the image?") is
answered by reading the generated prompt, not the C# around it.

.NET 10 console app, Autofac DI, no test framework — correctness is enforced by
self-checking smoke runs (below).

## Layout

```
src/LifeOverYears/
  Program.cs            CLI modes, config, project-root discovery
  AppModule.cs          DI wiring; reads Pipeline:*/OpenAi:* config
  Services/Pipeline.cs  the 5 steps: vision → prompts → base → era images → video
  Services/PromptService.cs   ~1350 lines; builds every prompt block. The core file.
  Services/PromptSmokeTest.cs C1–C56 checks over generated prompts
  Models/               SceneDna (from vision), EraProfile (from data/eras)
  data/eras/{year}.json  the six era profiles — most "why does the image look
                         like that in year N" answers live here, not in code
  data/prompts/*.txt     templates: image-template, base-clean, base-synthetic,
                         vision, caption-*
  data/brands/, data/captions/   sampling pools
  output/smoke/          committed smoke output — regenerated by the smoke run
```

Docs in `docs/` are conceptual design notes and drift from the code; trust the
code and this file first.

## Commands

```
cd src/LifeOverYears
dotnet run -- --smoke-prompts    # C1–C56 + folder checks; rewrites output/smoke/**
dotnet run -- --smoke-video      # ffmpeg/overlay, isolated
dotnet run -- --smoke-batch      # OpenAI batch provider against a fake
dotnet run -- run <photo> [years...]      # full pipeline
dotnet run -- collect <runFolder> [--wait]  # finish a batch run, assemble video
dotnet run -- assemble <runFolder> [years...]
dotnet run -- vision-variance <folder> [--repeat N]
```

`--smoke-prompts` takes ~1–2 min and needs no API key. **Always run it after
touching PromptService or data/eras** — and expect `git status` to show a large
diff under `src/LifeOverYears/output/smoke/`: that is the regenerated report,
not stray files.

## Config (`src/LifeOverYears/appsettings.json`, gitignored)

| key | default | note |
|---|---|---|
| `Pipeline:BaseMode` | `clean` | `clean` = source photo emptied; `synthetic` = scene rebuilt from SceneDna text, photo never sent. Currently `synthetic`. |
| `Pipeline:EraChaining` | `true` | **on by default.** Each era edits the *previous era's finished image*, not the shared base. |
| `OpenAi:Mode` | `sync` | `batch` for the batch API. Currently `batch`. |
| `Pipeline:InputDir/ProcessedDir/FailedDir/OutputDir` | `testImage`/`processed`/`failed`/`output/runs` | |

`EraChaining=true` is the single most load-bearing fact when reasoning about
images: anything visible in the 1975 frame carries forward through every later
frame unless a prompt explicitly asks for its removal. A block that merely
*omits* a feature does not delete it.

## Where the knobs are

**Tree size** — `PromptService.DescribeTreeSize`. Per-decade retention rates by
recorded size (large .90 / medium .78 / small .62), all divided by
`GrowthDamping` (0.95, a deliberate 5% slowdown, Aug 2026). Chained runs read as
growth against the uploaded previous era; unchained runs as a fraction of the
base image. The two paths are exact inverses — change retention, not one branch.
Percentages round to the nearest 5%. The source year (2025) emits no TREES
section at all.

**Utilities / poles / wires** — `PromptService.BuildEnvironmentBlock` plus
`utilities` in each `data/eras/{year}.json`. Only the **first two** entries of a
pool ever reach the prompt. Pool selection: `downtown_characteristics` for
`downtown_street`, `strip_mall_characteristics` for `strip_mall`, otherwise the
general `characteristics`. From 2015 those two scene types are undergrounded
(`"undergrounded": true`), which also emits an explicit "remove every utility
pole and wire span" line — required, because the base image and every earlier
chained frame still show poles. Gas stations and auto repair keep their wires.
Locked by check C56. Note this deliberately overrides the PRESERVE block; worth
eyeballing real 2015/2025 frames for pole stumps or smeared sky.

**Captions** — `data/captions/{sceneType}.txt`, falling back to `base.txt`. That
directory is the *only* home for caption text; nothing caption-related lives
outside `src/LifeOverYears/data/`. Bodies are separated by a line that is exactly
`---`, must use only the placeholders `{firstYear}` `{lastYear}` `{angle}`
`{condition}`, must carry the years and the condition (checked by C41/C55), must
end on a question, and must not contain hashtags — those are appended.
Which body a scene gets is deterministic: ISO week + a stable hash of the scene
id. `data/prompts/caption-*.txt` are legacy LLM system prompts, not the live path.

**Titles** — `data/captions/titles/{sceneType}.txt`, same `base.txt` fallback: one
YouTube hook per line, no `---` separators, only `{firstYear}` `{lastYear}` (a
title is too short for `{angle}`/`{condition}`), each under 100 chars and checked
by C59. `CaptionRunner` writes it to `title.txt`, kept apart from `caption.txt`,
which stays the Facebook/Instagram payload.

**Hashtags** — `data/captions/hashtags.txt`. The first three unweighted lines are
pinned and ship in file order; two more are sampled from the rest. A `NN%` suffix
(`#nostalgia 70%`) pulls a tag out of the pool onto its own roll at that
probability, spending a sampled slot when it wins so the post keeps five tags.
Editing the file is the whole interface — no code change needed to repin,
reweight or add tags. Locked by C57.

**Period details / props** — sampled from era `scene_content` (storefronts,
window_signs, extras) in `BuildSceneBlock`. Those pools know nothing about the
photo's geometry, so every prompt closes the block with
`PromptService.PlacementRule`: place each detail where it plausibly belongs, and
if there is nowhere for one, leave it out — nothing in the roadway or a driving
lane. PRIORITY ORDER item 4 in `image-template.txt` says the same at the top.
Without that escape clause the model plants every listed prop somewhere and you
get benches standing in the road. Locked by C58; add the same "or omit it"
wording to any new list of props.

**Scene types** — `gas_station`, `downtown_street`, `strip_mall`, `auto_repair`,
`mall`, `shopping_center`, plus a `default`/`unknown` fallback. Era JSONs key
`scene_content` by these names.

**Conditions** — `thriving/busy/new/declining/abandoned/squatted/restored`,
picked per era in `GenerationContext.PickSceneCondition` for gas_station,
downtown_street, strip_mall and auto_repair. The rank is monotonic across a run
(a place never quietly recovers mid-arc); only the last era resolves it.
Derelict eras replace the live-business PERIOD DETAILS block entirely.

## Conventions

- Comments explain *why*, in prose, at the decision point — not what the line
  does. Match that density; it is the house style.
- Every prompt behaviour worth keeping gets a numbered check in
  `PromptSmokeTest` (C1…C56, sequential). Add one when you change what prompts
  say; update the hard-coded expected strings when you change wording or numbers.
- Image models ignore bare negations. Say what *is* there, and when something
  must disappear, name the removal explicitly and say what replaces it.
- A list of details reads as mandatory unless the prompt says otherwise. Any new
  pool of props needs its own "if it has nowhere to go, leave it out" clause.
- Prompts have two hard budgets, both checked: 920 words (C11) and
  `MaxPromptChars` 6000 (C22). The worst case today is ~895 words / ~5650 chars,
  so there is room for roughly one more sentence — new lines must earn their
  space, and raising a limit is a deliberate act with a comment saying why.
